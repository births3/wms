import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = resolve(__dirname, "..");
const page = readFileSync(resolve(root, "src/pages/outbound/M4OutboundPage.tsx"), "utf8");
const queries = readFileSync(resolve(root, "src/features/outbound/outbound-queries.ts"), "utf8");
const devMockCore = readFileSync(resolve(root, "dev-mocks/web-admin-dev-mock-core.ts"), "utf8");
const devMock = readFileSync(resolve(root, "dev-mocks/outbound-dev-mock.ts"), "utf8");

assert.doesNotMatch(page, /待接真实 API 后拆 feature hooks/, "M4 页面豁免理由必须反映新建接口已接入");
assert.match(queries, /api\.POST\("\/api\/v1\/outbound\/orders"/, "M4 必须提供真实创建出库单 API 客户端");
assert.match(queries, /api\.GET\("\/api\/v1\/outbound\/orders"/, "M4 出库订单列表必须读取真实 API");
assert.match(queries, /api\.GET\("\/api\/v1\/outbound\/orders\/\{id\}"/, "M4 出库订单详情必须读取真实 API");
assert.match(queries, /api\.GET\("\/api\/v1\/outbound\/orders\/\{id\}\/review"/, "M4 复核弹窗必须读取真实复核明细 API");
assert.match(queries, /api\.POST\("\/api\/v1\/outbound\/orders\/\{id\}\/review"/, "M4 复核必须调用真实提交 API");
assert.match(queries, /api\.GET\("\/api\/v1\/outbound\/waves"/, "M4 波次列表必须读取真实 API");
assert.match(queries, /api\.GET\("\/api\/v1\/outbound\/waves\/\{wave_id\}"/, "M4 波次详情必须读取真实 API");
assert.match(queries, /useCreateOutboundOrderMutation/, "M4 必须提供创建出库单 mutation");
assert.match(queries, /api\.POST\("\/api\/v1\/outbound\/waves"/, "M4 波次必须提供真实创建 API 客户端");
assert.match(queries, /useCreateOutboundWaveMutation/, "M4 必须提供创建波次 mutation");
assert.match(queries, /useOutboundOrdersQuery/, "M4 必须提供出库订单列表 query");
assert.match(queries, /useOutboundOrderQuery/, "M4 必须提供出库订单详情 query");
assert.match(queries, /useOutboundReviewQuery/, "M4 必须提供出库复核详情 query");
assert.match(queries, /useReviewOutboundOrderMutation/, "M4 必须提供出库复核 mutation");
assert.match(queries, /Idempotency-Key/, "M4 创建出库单必须携带幂等键");
assert.match(page, /useCreateOutboundOrderMutation/, "M4 页面必须接入创建出库单 mutation");
assert.match(page, /useCreateOutboundWaveMutation/, "M4 页面必须接入创建波次 mutation");
assert.match(page, /useOutboundOrdersQuery/, "M4 页面必须接入出库订单列表 query");
assert.match(page, /useOutboundOrderQuery/, "M4 页面必须接入出库订单详情 query");
assert.match(page, /useOutboundReviewQuery/, "M4 页面必须接入出库复核详情 query");
assert.match(page, /useReviewOutboundOrderMutation/, "M4 页面必须接入出库复核 mutation");
assert.match(page, /useOutboundWavesQuery/, "M4 页面必须接入出库波次列表 query");
assert.match(page, /useOutboundWaveQuery/, "M4 页面必须接入出库波次详情 query");
assert.match(page, /orderDetailQuery\.data/, "M4 详情弹窗必须优先使用真实详情响应");
assert.match(page, /waveDetailQuery\.error/, "M4 波次详情读取失败必须回显错误");
assert.match(page, /await ordersQuery\.refetch\(\)/, "M4 刷新必须重新读取真实出库订单列表");
assert.match(page, /useSystemDictionaryItemOptionsQuery\("document_type"/, "M4 单据类型必须读取 M1 系统字典");
assert.match(page, /documentTypeOptions/, "M4 创建弹窗必须使用字典选项");
assert.doesNotMatch(page, /<option value="sales_outbound">|<option value="purchase_return_outbound">/, "M4 单据类型选项不得在页面硬编码");
assert.match(page, /await createOutboundOrderMutation\.mutateAsync/, "M4 新建动作必须等待真实 API 返回");
assert.match(page, /document_type:\s*createForm\.documentType/, "M4 创建请求必须提交单据类型");
assert.match(page, /wms_order_no:\s*createForm\.wmsOrderNo\.trim\(\)/, "M4 创建请求必须允许空单号交由后端生成");
assert.match(page, /setOrders\(\(value\) => \[created, \.\.\.value\]\)/, "M4 创建成功后必须把后端返回单据加入列表");
assert.match(page, /createOutboundOrderMutation\.error/, "M4 创建失败必须回显错误");
assert.match(page, /createOutboundWaveMutation\.error/, "M4 波次创建失败必须回显错误");
assert.match(page, /await createOutboundWaveMutation\.mutateAsync/, "M4 新建波次必须等待真实 API 返回");
assert.match(page, /order_ids: orderIds/, "M4 新建波次必须提交订单选择");
assert.match(page, /review_mode:\s*"packing_station"/, "M4 PC 复核必须提交包装站复核模式");
assert.match(page, /reviewer_id:\s*reviewerId/, "M4 复核必须提交当前登录用户");
assert.match(page, /reviewed_qty:\s*line\.picked_qty/, "M4 复核数量必须来自真实复核明细");
assert.doesNotMatch(page, /if \(action\.kind === "review"\) updateOrder/, "M4 复核不得使用本地 updateOrder 演示成功");
assert.match(page, /reviewOutboundOrderMutation\.mutateAsync/, "M4 复核提交必须等待真实 API 返回");
assert.match(page, /setOrders\(\(value\) => value\.map\(\(item\) => item\.id === reviewed\.id \? reviewed : item\)\)/, "M4 复核成功后必须使用后端返回订单更新列表");
assert.match(page, /reviewDetailQuery\.error/, "M4 复核明细读取失败必须回显真实错误");
assert.match(devMockCore, /handleOutboundDevMock/, "M4 dev mock 主入口必须接入出库路由");
assert.match(devMock, /POST.*\/api\/v1\/outbound\/orders|pathname === "\/api\/v1\/outbound\/orders"/, "M4 dev mock 必须覆盖创建出库单接口");
assert.match(devMock, /const detailMatch = pathname\.match/, "M4 dev mock 必须覆盖出库订单详情接口");
assert.match(devMock, /const waveDetailMatch = pathname\.match/, "M4 dev mock 必须覆盖出库波次详情接口");
assert.match(devMock, /pathname === "\/api\/v1\/outbound\/waves"/, "M4 dev mock 必须覆盖出库波次列表接口");
assert.match(devMock, /sendJson\(res, 200, order\)/, "M4 dev mock 详情接口必须返回订单数据");
assert.match(devMock, /document_type/, "M4 dev mock 必须保留单据类型");
assert.match(devMock, /POST.*\/api\/v1\/outbound\/waves|pathname === "\/api\/v1\/outbound\/waves"/, "M4 dev mock 必须覆盖创建波次接口");
assert.match(devMock, /order_ids/, "M4 dev mock 波次接口必须保留订单选择");

console.log("m4 outbound create api self-check passed");
